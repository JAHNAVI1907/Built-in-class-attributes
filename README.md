# Built-in-class-attributes
built in class attr 1).__doc__ --- when string doc is not specified nreturn attr 2).__dict__ -- namespace accessed attributes 3).__name__ --returns class attr's name 4).__module__ 5).__bases__

# Attributes and Abstraction in Python 🐍

This repository contains examples that explain **object attributes** and **abstraction concepts** in Python using simple programs and text explanations.

---

## 📂 Files Included

### 1. `builtin_attr.py`
Demonstrates the use of **built-in attribute functions** in Python such as:
- `getattr()`
- `setattr()`
- `hasattr()`
- `delattr()`

---

### 2. `getattr().txt`
Explains how the `getattr()` function works with examples.

---

### 3. `setattr().txt`
Explains how to dynamically set attributes using `setattr()`.

---

### 4. `hasattr().txt`
Explains how to check if an object has a specific attribute using `hasattr()`.

---

### 5. `delattr().txt`
Describes how to delete attributes from objects using `delattr()`.

---

### 6. `isinstance.txt`
Explains the use of `isinstance()` to check object types.

---

### 7. `issubcls.txt`
Explains how `issubclass()` works for checking class inheritance relationships.

---

### 8. `rich comparison methods.txt`
Explains **rich comparison methods** in Python:
- `__lt__()`
- `__gt__()`
- `__eq__()`
- `__le__()`
- `__ge__()`
- `__ne__()`

---

### 9. `Data Abstract.txt`
Explains the concept of **Data Abstraction** in Python.

---

### 10. `Abstract class for Payment System.txt`
Contains an example design of an **abstract class** for a payment system using the `abc` module.

---

## ▶ How to Use

You can:
- Open `.txt` files to read theoretical explanations
- Run the `.py` files to see practical implementation

Run Python file like this:

```bash
python builtin_attr.py

